let pessoa = {
  nome: "Maria",
  idade: 21,
  generoMusicalFavorito: "pop",
};

console.log(
  `O nome da pessoa é ${pessoa.nome}, ela tem ${pessoa.idade} e gosta muito de ${pessoa.generoMusicalFavorito}`
);
